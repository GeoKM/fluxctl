# Test fixtures

Drop reusable SCP samples or JSON layout artifacts here so pytest cases can share deterministic inputs without relying on tmp_path-generated data. Keep files small and document their intent in this README.
