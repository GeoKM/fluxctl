# Capability Registry

This file is generated by `scripts/generate_capability_docs.py`; edit `src/fluxctl/capabilities.py` instead.

| Filesystem | Containers | Detect | List | Extract | Mutations | Map overlay | HEX edit | Destinations | Limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FAT12 (`fat12`) | .img | Yes | Yes | Yes | create_directory, delete_entry, import_directory, import_file, replace_file | cluster-chain overlay | Yes | .raw, .imd | Write operations create a new flat .img copy and use 8.3 ASCII names. |
| CP/M variants (`cpm`) | .img | Yes | Yes | Yes | delete_entry, import_file | allocation-block overlay | No | .raw, .imd | Mutation is limited to modelled flat .img DPBs; replace and directory operations are not implemented. |
| CBM DOS 1541 (`cbm_dos`) | .d64 | Yes | Yes | Yes | delete_entry, import_file, replace_file | CBM DOS BAM block map | No | .d64, .raw | Root-level file operations only; directory import and creation are not implemented. |
| CBM DOS 1571 (`cbm_dos_1571`) | .d71 | Yes | Yes | Yes | delete_entry, import_file, replace_file | CBM DOS BAM block map | No | .d71, .raw | Root-level file operations only; directory import and creation are not implemented. |
| CBM DOS 1581 (`cbm_dos_1581`) | .d81 | Yes | Yes | Yes | create_directory, delete_entry, import_directory, import_file, replace_file | 1581 BAM block map | No | .d81, .raw | Replace and delete remain root-file-only; REL side-sector mutation is not implemented. |
| Amiga OFS/FFS (`amiga_ffs`) | .adf | Yes | Yes | Yes | None | filesystem logical map | No | .adf, .raw | ADF mutation is pending allocation bitmap, checksums, file headers, and directory hash-chain updates. |
| Apple ProDOS (`prodos`) | .woz, .po, .do, .nib, .img, .scp | Yes | Yes | Yes | None | ProDOS block overlay | No | .po, .do | Read-only Apple II support. |
| Apple DOS 3.3 (`apple_dos_33`) | .woz, .po, .do, .nib, .img, .scp | Yes | Yes | Yes | None | DOS 3.3 T/S-list overlay | No | .do | Read-only; extracted sizes are sector-granular. |
| DisplayWriter (`displaywriter`) | .img, .scp, .imd | Yes | Yes | No | None | None | No | .raw | Only standard-label records are listed; document extraction is not implemented. |
| RT-11 (`rt11`) | .img, .scp, .imd | Yes | Yes | Yes | None | None | No | .raw | Read-only flat RAD50 directory and extent reader. |
| Wang OIS (`wang_ois`) | .img, .scp | Yes | Yes | Yes | None | allocation extent overlay | No | .raw | Read-only support for the modelled 315K package catalog. |
| Seiko 8300 (`seiko_8300`) | .img, .scp | Yes | Yes | No | None | None | No | .raw | Catalog/header records are readable; physical allocation is not proven. |
